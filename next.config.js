/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ['example.com'],
  },
};

module.exports = {
  images: {
    domains: ['example.com'], // 외부 이미지 도메인을 여기에 추가하세요
  },
};

module.exports = nextConfig;
